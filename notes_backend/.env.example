# Example environment configuration for notes_backend

# Database dialect: 'sqlite', 'mysql', 'postgres', etc.
DB_DIALECT=sqlite
# (If using sqlite, path to storage file, leave unset for in-memory)
DB_STORAGE=./notes.sqlite
# (If not sqlite, set the following)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=notes_db
DB_USER=exampleuser
DB_PASS=examplepass

# Node environment (optional)
NODE_ENV=development

# Server host (port always 3001 in server.js)
HOST=0.0.0.0
