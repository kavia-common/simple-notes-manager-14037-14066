# Example environment variables for notes_backend

# Database config
DB_DIALECT=sqlite
DB_STORAGE=./notes.sqlite

# Express server host (optional)
# HOST=0.0.0.0

# Node environment
NODE_ENV=development
